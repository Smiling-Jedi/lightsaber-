# 光剑系统 Bug 自查报告

> 自查时间：2026-03-24
> 检查范围：B+D方案相关代码、信号服务、前端模板

---

## ✅ 已修复的Bug

### Bug-001：持仓页信号区不显示 → ✅ 已修复

**原因**：浏览器缓存导致JS未更新，实际代码正常
**解决**：添加版本标记 + 无痕模式测试确认

---

### Bug-002：A股未配置分类应显示占位提示 → ✅ 已修复

**解决**：
1. 页面加载时先显示「正在计算信号...」
2. API返回后渲染有信号的股票
3. 未配置的股票（A股）显示「🔧 信号配置中」

---

### Bug-003：JS字符串替换只替换第一个 → ✅ 已修复

**修复**：`positions.html:826` `.replace('_', ':')` → `.replace(/_/g, ':')`

---

## 🔴 确认Bug（需修复）

（暂无待修复Bug）

---

## 🟡 潜在问题（建议优化）

### Issue-001：API响应慢导致前端loading时间过长

**现象**：`/api/signals/portfolio` 可能超时

**原因**：生成19只股票的信号需要拉取历史数据、计算指标

**建议**：
1. 添加缓存机制（当日信号缓存）
2. 异步生成信号，前端轮询结果
3. 或先返回空数据，前端单独拉取每只股票的信号

**优先级**：🟡 优化体验

---

### Issue-002：`renderSignalRight` 对缺失instruction处理不完整

**位置**：`positions.html` 第644-648行

**代码**：
```javascript
const instruction = sig.instruction || {};
const sizingModel = instruction.sizing_model || 'KELLY_HALF';
```

**问题**：如果 `sig.instruction` 为 `null`，`instruction` 是 `{}`，`sizing_model` 会是 `undefined`，不是 `'KELLY_HALF'`

**实际代码已处理**：`const sizingModel = instruction.sizing_model || 'KELLY_HALF';` 会回退到默认值

**状态**：✅ 代码已正确处理

---

### Issue-003：`renderSignalRight` 第682行可能返回空字符串

**代码**：
```javascript
if (!stopStr && !target1Str) return '';
```

**问题**：如果股票没有止损和目标（如HOLD信号），右侧盈利目标区完全空白

**建议**：即使无目标也应显示"无活跃交易计划"等占位内容

**优先级**：🟢 低

---

## ✅ 已验证正常的项

| 检查项 | 状态 |
|--------|------|
| Python语法检查 | ✅ 通过 |
| 模型字段（分批建仓） | ✅ 完整 |
| SIZING_MODEL常量定义 | ✅ 已定义 |
| API返回数据结构 | ✅ 正常 |
| HTML元素ID生成 | ✅ 正确 |

---

## 下一步行动建议

**Jedi，请选择优先修复哪些：**

1. **Bug-001（信号不显示）** - 需要浏览器Console报错信息才能定位
2. **Bug-003（字符串替换）** - 我可以立即修复
3. **Issue-001（API慢）** - 需要设计缓存方案

建议先修复Bug-003（简单），然后针对Bug-001，你可以：
- 按 `Cmd + Option + J` 打开Console看红色报错
- 或者让我添加更多日志来定位问题
