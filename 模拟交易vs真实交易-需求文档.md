# 模拟交易 vs 真实交易 · 需求文档

> 所属系统：光剑系统 · 个股详情模块
> 讨论日期：2026-03-21
> 状态：需求确认，待开发

---

## 一、背景与目标

每次原力模块发出交易信号（BUY/SELL），自动假设"已按信号执行"，持续记录模拟交易结果。
同期与真实交易记录并排对比，回答核心问题：

> **"如果每次信号都跟了，策略实际表现如何？我的真实操作和它差了多少？"**

---

## 二、核心原则

- **起跑线相同**：模拟盘初始持仓从真实持仓快照一次，时间点相同
- **独立存储**：此后模拟持仓和真实持仓完全独立，互不影响
- **只做多头**：SELL 信号有对应模拟持仓则平仓，无则忽略，不模拟做空
- **数据优先富途**：本模块所有行情和成交数据，优先调用富途 OpenD 接口

---

## 三、页面呈现（个股详情页）

```
┌───────────────────────────────────────────────────────┐
│  个股详情 · 腾讯(00700)                                │
├───────────────────────────────────────────────────────┤
│                                                       │
│  [K线图 · ECharts · 过去1个月日K]                     │
│                                                       │
│   B1 / S1 ── 真实交易打点（蓝色实心）                  │
│   B2 / S2 ── 模拟交易打点（紫色空心）                  │
│                                                       │
│   悬停 Tooltip：价格 / 股数 / 时间 / 收益率            │
│                                                       │
├────────────────────┬──────────────────────────────────┤
│  📊 实盘交易记录    │  🔮 模拟交易记录                 │
│                    │                                  │
│  胜率 / EV / 盈亏比 │  胜率 / EV / 盈亏比              │
│  ──────────────    │  ──────────────                  │
│  日期|方向|价格     │  日期|方向|价格                  │
│  股数|盈亏%        │  股数|盈亏%                      │
└────────────────────┴──────────────────────────────────┘
```

---

## 四、数据来源

| 数据 | 来源 | 说明 |
|------|------|------|
| K线 OHLCV | 富途 `get_history_kline` | 日K，QFQ前复权，过去1个月 |
| 真实成交记录 | 富途 `deal_list_query` | 每日收盘后定时同步到本地 |
| 模拟交易记录 | 本地数据库 | 信号触发时自动写入，无需富途 |

---

## 五、模拟持仓管理

### 5.1 初始化（一次性快照）

- 运行初始化脚本，从真实持仓表复制到模拟持仓表
- 之后两者完全独立，真实持仓变化不影响模拟持仓
- 初始化只可执行一次（已存在则跳过）

### 5.2 自动入场规则

触发条件：原力模块生成 BUY 信号

- 入场价：信号当日收盘价
- 入场股数：按回测参数中的 `kelly_pct` 计算（基于当前模拟总资产）
- 自动更新模拟持仓的持股数 / 平均成本价

### 5.3 自动出场规则

每日价格刷新后自动检查，优先级：止损 > 止盈 > 到期

| 条件 | 出场状态 |
|------|---------|
| 当日最低价 ≤ 入场价 × (1 + 止损%) | HIT_STOP |
| 当日最高价 ≥ 入场价 × (1 + 目标%) | HIT_TARGET |
| 持有时长 > hold_months 月 | EXPIRED |

SELL 信号出现时：
- 有对应模拟持仓（做多）→ 强制平仓
- 无对应持仓 → 忽略

### 5.4 模拟持仓表字段

```
symbol, name, category
shares          # 模拟持有股数
avg_cost        # 平均成本价
last_price      # 最新价（每日更新）
market_value    # 当前市值
created_at, updated_at
```

---

## 六、真实成交同步

- 每日收盘后，与行情刷新脚本一起触发
- 调用富途 `deal_list_query` 拉取历史成交流水
- 写入本地 `trade` 表，以 `futu_order_id` 做幂等去重（重复不写）
- 新增字段：`trade.futu_order_id`

---

## 七、统计对比指标

两套记录各自独立统计以下指标，用于并排对比：

| 指标 | 说明 |
|------|------|
| 胜率 | 盈利交易笔数 / 总交易笔数 |
| EV（期望值%） | 平均每笔期望收益率 |
| 盈亏比 | 平均盈利% / 平均亏损% |
| 最大连续亏损 | 历史最多连亏几笔 |
| 总盈亏% | 累计收益率 |

---

## 八、技术改动范围

### 新建文件

| 文件 | 用途 |
|------|------|
| `app/models/sim_position.py` | 模拟持仓表模型 |
| `app/services/futu_kline_service.py` | 富途K线拉取 + 本地缓存 |
| `app/services/futu_deal_sync_service.py` | 富途历史成交同步 |
| `scripts/init_sim_positions.py` | 一次性初始化模拟持仓快照 |
| `scripts/migrate_add_simulated.py` | 数据库迁移：加 is_simulated 字段 |

### 修改文件

| 文件 | 改动内容 |
|------|---------|
| `app/models/signal_log.py` | 加 `is_simulated` 字段 |
| `app/models/trade.py` | 加 `futu_order_id` 字段 |
| `app/services/signal_log_service.py` | 新增模拟入场/出场方法，扩展绩效统计 |
| `app/routers/api_router.py` | 新增 K线接口、交易对比接口 |
| `app/templates/detail.html` | ECharts K线图 + 双套打点 + 双栏交易记录 |
| `scripts/refresh_prices.py` | 每日触发模拟出场检查 + 富途成交同步 |

---

## 九、待确认事项

- [ ] 富途 `deal_list_query` 实际可查的历史范围（建议上线前测试一次）
- [ ] Kelly 仓位计算基准：用模拟总资产，还是固定初始总资产

---

## 十、不在本期范围内

- 模拟做空
- 分批建仓的模拟（本期统一按 kelly_pct 一次性入场）
- 模拟持仓的实时盈亏页面（本期只在个股详情页展示）
