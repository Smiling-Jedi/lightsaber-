# 新闻模块 —— 需求与架构文档

> 所属系统：光剑系统（Lightsaber）
> 创建时间：2026-03-21
> 当前版本：v0.2
> 状态：UI设计中，需求已确认，待 UI 完成后实现

## 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| v0.1 | 2026-03-21 | 初版草稿，记录待讨论问题和调研结论 |
| v0.2 | 2026-03-21 | 完成所有待讨论问题的决策，确认实现方案 |

---

## 一、调研结论（已确认）

### 现有基础
- News 表已有：title / summary / url / source / published_at / stock_symbol
- 去重逻辑：按 url 字段去重
- 手动触发：点击"更新资讯"按钮 → `POST /api/news/update`
- 每只股票每次最多拉 5 条，保留 30 天

### 已发现 Bug（待修复）
- **港股新闻完全失效**：`news_service.py` 第37行在传 symbol 前剥掉市场前缀，`HK:00700` → `"00700"` → `_symbol_to_yf()` 无法正确转换
- **根本原因**：`news_source.py` 的 `_symbol_to_yf()` 需要完整 symbol（含市场前缀），`news_service.py` 不应提前剥离
- **修复方案**：`news_service.py` 第37行直接传 `stock.symbol`，不做剥离
- **美股不受影响**：`US:TSLA` → `_symbol_to_yf()` → `"TSLA"` 本就正确

### 新闻源（已确认）
| 来源 | 港股 | 美股 | 说明 |
|------|------|------|------|
| yfinance | ✅ 修复后可用 | ✅ 正常 | Bug修复后港美股统一，英文内容 → LLM 翻译 |
| 东方财富 | ❌ 不采用 | — | 验证发现接口不稳定，yfinance 够用 |

验证结果：`0700.HK`（腾讯）、`9988.HK`（阿里）各返回 10 条英文新闻，内容相关性高。

---

## 二、需求决策（已确认）

### 2.1 新闻源
✅ **yfinance 单一来源**，港美股统一处理，修复 symbol 格式 bug 即可。

### 2.2 重要度分级
✅ **方案B：LLM 判断**（claude-haiku），每条新闻一次调用同时完成：
- 翻译英文标题 → 中文标题（存入 `title_zh` 字段）
- 判断重要度：HIGH / MEDIUM / LOW
  - HIGH：财报/盈警/监管处罚/并购/评级调整/大股东增减持
  - MEDIUM：行业政策、管理层动态、产品发布等有一定影响的消息
  - LOW：泛泛分析、无实质内容的推测类文章

显示给用户：只显示 HIGH 和 MEDIUM，不展示 LOW。

### 2.3 显示位置
✅ **方案A**：每张持仓卡内显示该股 TOP 3 重要新闻（按 importance 降序、时间降序）。

### 2.4 刷新机制
✅ **定时 + 手动双轨**：

**自动定时（APScheduler）：**
| 触发时间 | 说明 |
|---------|------|
| 港股收盘后：16:15 HKT | 港股交易日结束后拉取 |
| 美股收盘后：05:30 HKT（次日） | 美股交易日结束后拉取 |

**手动触发：**
- 页面现有"更新资讯"按钮 → `POST /api/news/update` 保留并继续可用
- 手动触发与自动触发逻辑相同（拉取 → LLM 处理 → 存库）

### 2.5 数据库字段扩展
✅ News 表新增：
- `title_zh`：中文标题（LLM 翻译结果）
- `importance`：重要度枚举（HIGH / MEDIUM / LOW）

不加 `event_type`（当前阶段不需要，后续可扩展）。

---

## 三、实现步骤（待 UI 完成后执行）

| 步骤 | 文件 | 内容 |
|------|------|------|
| 1 | `news_service.py` | Bug fix：第37行不剥市场前缀，直接传 `stock.symbol` |
| 2 | `app/models/news.py` | 新增 `title_zh` 和 `importance` 字段 + DB migration |
| 3 | `app/services/news_process_service.py` | 新建：haiku 一次调用完成翻译 + 重要度打分 |
| 4 | `news_service.py` | `fetch_all_news` 拉取上限改为每股 10 条，调用 LLM 处理后存库 |
| 5 | `app/scheduler.py` | APScheduler 定时任务：港股 16:15 / 美股次日 05:30 自动触发 |
| 6 | `positions.html` | 新闻区渲染：显示 TOP 3，用 `title_zh`，按 importance 排序 |

---

## 四、已有代码位置

| 文件 | 说明 |
|------|------|
| `app/models/news.py` | News 数据模型 |
| `app/services/news_service.py` | 新闻拉取、去重、存储 |
| `app/data_sources/news_source.py` | yfinance 新闻源实现（含正确的 `_symbol_to_yf`） |
| `app/routers/api_router.py` | `POST /api/news/update` 手动触发接口 |
